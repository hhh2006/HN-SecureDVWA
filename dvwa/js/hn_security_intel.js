(()=>{'use strict';const $=s=>document.querySelector(s);const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){document.querySelectorAll('.si-stat strong,.report-score strong').forEach(el=>{const n=parseInt(el.textContent,10);if(Number.isFinite(n)){el.textContent='0';let st=performance.now();const f=t=>{const p=Math.min(1,(t-st)/800),e=1-Math.pow(1-p,3);el.textContent=String(Math.round(n*e));if(p<1)requestAnimationFrame(f)};requestAnimationFrame(f)}})}const pr=$('#si-print');if(pr)pr.addEventListener('click',()=>window.print());const rt=$('#si-report-time');if(rt){const d=new Date();rt.textContent=d.toLocaleString();}document.querySelectorAll('.si-control-card,.assurance-card,.si-control').forEach((el,i)=>{if(reduce)return;el.style.animationDelay=(i*45)+'ms';el.classList.add('si-reveal')});})();
